// Per-game round history list — implemented in Prompt 7
'use client'

export function RoundHistory() {
  return <div style={{ color: 'var(--text-secondary)' }}>RoundHistory — coming in Prompt 7</div>
}
