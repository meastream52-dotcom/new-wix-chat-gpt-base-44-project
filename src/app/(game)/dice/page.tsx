// Dice game — implemented in Prompt 5
export default function DicePage() {
  return (
    <main className="min-h-screen p-8" style={{ background: 'var(--bg-primary)' }}>
      <h1 className="text-3xl font-bold mb-2" style={{ color: 'var(--text-primary)' }}>Dice</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Game — coming in Prompt 5</p>
    </main>
  )
}
