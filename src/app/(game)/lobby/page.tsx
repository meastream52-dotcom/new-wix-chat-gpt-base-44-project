// Lobby — implemented in Prompt 6
export default function LobbyPage() {
  return (
    <main className="min-h-screen p-8" style={{ background: 'var(--bg-primary)' }}>
      <h1 className="text-3xl font-bold mb-2" style={{ color: 'var(--text-primary)' }}>Casino Lobby</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Games — coming in Prompt 6</p>
    </main>
  )
}
