// Login page — implemented in Prompt 3
export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: 'var(--bg-primary)' }}>
      <div style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }} className="p-8 rounded-xl w-full max-w-md">
        <h1 className="text-2xl font-bold mb-6" style={{ color: 'var(--text-primary)' }}>Sign In</h1>
        <p style={{ color: 'var(--text-secondary)' }}>Auth — coming in Prompt 3</p>
      </div>
    </div>
  )
}
